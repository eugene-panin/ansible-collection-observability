---
- name: Converge
  hosts: all
  become: true
  vars:
    grafana_version: 13.2.1
    grafana_admin_password: first-password
  roles:
    - role: "{{ lookup('ansible.builtin.env', 'MOLECULE_PROJECT_DIRECTORY') }}"
